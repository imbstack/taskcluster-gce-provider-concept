{
  "name": "taskcluster-gce-provider-test",
  "version": "1.0.0",
  "private": true,
  "description": "TODO",
  "license": "MPL-2.0",
  "scripts": {
    "heroku-prebuild": "echo $SOURCE_VERSION > .git-version",
    "test": "mocha test/*_test.js",
    "lint": "eslint src/*.js test/*.js",
    "pretest": "yarn lint"
  },
  "dependencies": {
    "googleapis": "^32.0.0",
    "jsonwebtoken": "^8.3.0",
    "lodash": "^4.11.1",
    "taskcluster-client": "^11.0.1",
    "taskcluster-lib-api": "^12.3.0",
    "taskcluster-lib-app": "^10.0.0",
    "taskcluster-lib-docs": "^10.0.1",
    "taskcluster-lib-loader": "^10.0.1",
    "taskcluster-lib-validate": "^11.0.2",
    "typed-env-config": "^2.0.0"
  },
  "engine-strict": true,
  "engines": {
    "node": "^10.0.0",
    "yarn": "^1.0.0"
  }
}
